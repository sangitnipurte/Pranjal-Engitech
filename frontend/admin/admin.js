const API=(window.PRANJAL_CONFIG?.API_BASE||'').replace(/\/$/,'');let token=localStorage.getItem('pranjal_admin_token');let site={};const $=s=>document.querySelector(s);const api=async(path,opts={})=>{opts.headers=opts.headers||{};if(token)opts.headers.Authorization='Bearer '+token;let r;try{r=await fetch(API+path,opts)}catch(err){throw new Error(`Cannot connect to backend API at ${API||'the configured API URL'}. Start Flask on port 5000 or check the Render URL.`)}let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d};const toast=m=>{const t=$('#toast');t.textContent=m;t.classList.remove('hidden');setTimeout(()=>t.classList.add('hidden'),2600)};const esc=s=>String(s??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));const image=v=>{if(!v)return '../assets/product-placeholder.svg';if(/^https?:\/\//i.test(v))return v;if(v.startsWith('/static/'))return API+v;if(v.startsWith('static/'))return API+'/'+v;if(v.startsWith('images/'))return API+'/static/'+v;if(v.startsWith('uploads/'))return API+'/static/'+v;return v};
async function login(e){e.preventDefault();try{const d=await api('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:$('#username').value,password:$('#password').value})});token=d.token;localStorage.setItem('pranjal_admin_token',token);showApp();loadTab('dashboard')}catch(err){$('#loginError').textContent=err.message;$('#loginError').classList.remove('hidden')}}function showApp(){$('#login').classList.add('hidden');$('#app').classList.remove('hidden')}function logout(){localStorage.removeItem('pranjal_admin_token');location.reload()}
const titles={dashboard:'Dashboard',settings:'Website Settings',categories:'Header Categories',products:'Products',clients:'Our Clients',events:'Events & Exhibitions',enquiries:'Enquiries'};
async function loadTab(tab){document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$('#pageTitle').textContent=titles[tab]||tab;try{if(tab==='dashboard')await dashboard();if(tab==='settings')await settings();if(tab==='categories')await categories();if(tab==='products')await products();if(tab==='clients')await clients();if(tab==='events')await events();if(tab==='enquiries')await enquiries()}catch(e){if(e.message.toLowerCase().includes('authentication'))logout();else $('#content').innerHTML='<div class="admin-panel"><div class="notice error">'+esc(e.message)+'</div></div>'}}
async function dashboard(){const s=await api('/api/admin/stats');$('#content').innerHTML='<div class="stats">'+['products','categories','clients','events','offers','enquiries'].map(k=>`<div class="stat"><span>${k}</span><b>${s[k]||0}</b></div>`).join('')+'</div><div class="admin-panel" style="margin-top:20px"><h3>Quick Actions</h3><div class="toolbar"><button class="btn btn-primary" onclick="loadTab(\'products\')">Manage Products</button><button class="btn btn-outline" onclick="loadTab(\'categories\')">Header Categories</button><button class="btn btn-outline" onclick="loadTab(\'events\')">Events & Exhibitions</button><button class="btn btn-outline" onclick="loadTab(\'clients\')">Manage Clients</button></div></div>'}
async function settings(){site=await api('/api/site');$('#content').innerHTML=`<div class="admin-panel"><h3>Website Settings</h3><p class="section-note">Update branding, contact details and the homepage hero from here.</p><form id="settingsForm" class="admin-form" enctype="multipart/form-data"><div><label>Company Name</label><input name="company_name" value="${esc(site.company_name)}"></div><div><label>Short Name</label><input name="short_name" value="${esc(site.short_name)}"></div><div><label>Tagline</label><input name="tagline" value="${esc(site.tagline)}"></div><div><label>Phone</label><input name="phone" value="${esc(site.phone)}"></div><div><label>Email</label><input name="email" value="${esc(site.email)}"></div><div><label>Hours</label><input name="hours" value="${esc(site.hours)}"></div><div class="full"><label>Address</label><textarea name="address">${esc(site.address)}</textarea></div><div class="full"><label>Announcement</label><input name="announcement" value="${esc(site.announcement)}"></div><div class="full"><label>Hero Title</label><input name="hero_title" value="${esc(site.hero_title)}"></div><div class="full"><label>Hero Text</label><textarea name="hero_text">${esc(site.hero_text)}</textarea></div><div class="full"><label>About Title</label><input name="about_title" value="${esc(site.about_title)}"></div><div class="full"><label>About Text</label><textarea name="about_text">${esc(site.about_text)}</textarea></div><div><label>Logo</label><input type="file" name="logo_image" accept="image/*"></div><div><label>Hero Image</label><input type="file" name="hero_image" accept="image/*"></div><div class="full toolbar"><button class="btn btn-primary">Save Website Settings</button><button type="button" class="btn btn-outline" onclick="loadTab('settings')">Discard Changes</button></div></form></div>`;$('#settingsForm').onsubmit=async e=>{e.preventDefault();try{site=await api('/api/site',{method:'PUT',body:new FormData(e.target)});toast('Website settings saved');}catch(err){toast(err.message)}}}
async function categories(){const cs=await api('/api/admin/categories');$('#content').innerHTML=`<div class="admin-panel"><h3>Header Categories</h3><p class="section-note">These names appear in the blue category bar below the main header. Rename or delete them here. Renaming also updates products assigned to that category.</p><form id="categoryForm" class="admin-form" enctype="multipart/form-data"><input type="hidden" name="id"><div><label>Category Name</label><input name="name" required placeholder="FLANGE GUARDS"></div><div><label>Sort Order</label><input name="sort_order" type="number" value="0"></div><div><label>Image (optional)</label><input name="image" type="file" accept="image/*"></div><div><label><input name="active" type="checkbox" checked> Show in header</label></div><div class="full"><label>Description</label><textarea name="description"></textarea></div><div class="full toolbar"><button class="btn btn-primary">Save Category</button><button type="button" class="btn btn-success" id="addCategory">Add New Category</button><button type="button" class="btn btn-outline" id="discardCategory">Discard Changes</button></div></form></div><div class="admin-panel"><h3>Category List</h3><table class="admin-table"><thead><tr><th>Name</th><th>Order</th><th>Status</th><th>Action</th></tr></thead><tbody>${cs.map(c=>`<tr><td><b>${esc(c.name)}</b></td><td>${c.sort_order||0}</td><td>${c.active?'Visible':'Hidden'}</td><td><button class="btn-small" onclick='editCategory(${JSON.stringify(c).replace(/'/g,"&#39;")})'>Edit</button> <button class="btn-small btn-danger" onclick="deleteCategory('${c.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan="4">No categories.</td></tr>'}</tbody></table></div>`;const f=$('#categoryForm');$('#addCategory').onclick=()=>newCategory();$('#discardCategory').onclick=()=>newCategory();f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),id=fd.get('id');fd.delete('id');try{await api('/api/categories'+(id?'/'+id:''),{method:id?'PUT':'POST',body:fd});toast(id?'Category updated':'Category added');await categories()}catch(err){toast(err.message)}};newCategory(false)}
function newCategory(scroll=true){const f=$('#categoryForm');if(!f)return;f.reset();f.id.value='';f.active.checked=true;if(scroll)window.scrollTo({top:0,behavior:'smooth'})}function editCategory(c){const f=$('#categoryForm');f.id.value=c.id;f.name.value=c.name||'';f.sort_order.value=c.sort_order||0;f.description.value=c.description||'';f.active.checked=!!c.active;window.scrollTo({top:0,behavior:'smooth'})}async function deleteCategory(id){if(!confirm('Delete this header category? Products will remain but the category will disappear from the header.'))return;try{await api('/api/categories/'+id,{method:'DELETE'});toast('Category deleted');categories()}catch(e){toast(e.message)}}
async function products(){const ps=await api('/api/products');const cs=await api('/api/categories');$('#content').innerHTML=`<div class="admin-panel"><h3>Product Manager</h3><p class="section-note">Create a new product, edit an existing one, upload its image, or delete it. The catalogue is unchanged unless you edit it.</p><div class="toolbar" style="margin-bottom:18px"><button class="btn btn-success" id="addProduct">＋ Add New Product</button></div><form id="productForm" class="admin-form" enctype="multipart/form-data"><input type="hidden" name="id"><div><label>Product Name</label><input name="name" required></div><div><label>Category</label><select name="category">${cs.map(c=>`<option value="${esc(c.name)}">${esc(c.name)}</option>`).join('')}</select></div><div><label>Sort Order</label><input type="number" name="sort_order" value="0"></div><div><label>Product Image</label><input type="file" name="image" accept="image/*"></div><div class="full"><label>Description</label><textarea name="description"></textarea></div><div class="full"><label>Specifications / Notes</label><textarea name="specs">Available sizes and dimensions as per customer requirement.</textarea></div><div><label><input type="checkbox" name="active" checked> Active on website</label></div><div class="full toolbar"><button class="btn btn-primary" type="submit">Save Product</button><button class="btn btn-success" type="button" id="addProduct2">＋ Add New Product</button><button class="btn btn-outline" type="button" id="discardProduct">Discard Changes</button></div></form></div><div class="admin-panel"><h3>Product List (${ps.length})</h3><table class="admin-table"><thead><tr><th>Image</th><th>Category</th><th>Name</th><th>Status</th><th>Action</th></tr></thead><tbody>${ps.map(p=>`<tr><td><img class="thumb" src="${image(p.image)}" alt=""></td><td>${esc(p.category)}</td><td>${esc(p.name)}</td><td>${p.active?'Active':'Hidden'}</td><td><button class="btn-small" onclick='editProduct(${JSON.stringify(p).replace(/'/g,"&#39;")})'>Edit</button> <button class="btn-small btn-danger" onclick="deleteProduct('${p.id}')">Delete</button></td></tr>`).join('')}</tbody></table></div>`;const f=$('#productForm');const fresh=()=>{f.reset();f.id.value='';f.active.checked=true;f.sort_order.value=0;if(cs[0])f.category.value=cs[0].name;f.name.focus();window.scrollTo({top:0,behavior:'smooth'})};$('#addProduct').onclick=fresh;$('#addProduct2').onclick=fresh;$('#discardProduct').onclick=()=>{f.reset();f.id.value='';f.active.checked=true;if(cs[0])f.category.value=cs[0].name;toast('Changes discarded; form is ready for a new product')};f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),id=fd.get('id');fd.delete('id');try{const saved=await api('/api/products'+(id?'/'+id:''),{method:id?'PUT':'POST',body:fd});toast(id?'Product updated':'New product added');await products()}catch(err){toast(err.message)}};fresh()}
function editProduct(p){const f=$('#productForm');f.id.value=p.id;f.name.value=p.name||'';f.category.value=p.category||'';f.sort_order.value=p.sort_order||0;f.description.value=p.description||'';f.specs.value=p.specs||'';f.active.checked=!!p.active;window.scrollTo({top:0,behavior:'smooth'})}async function deleteProduct(id){if(!confirm('Delete this product?'))return;try{await api('/api/products/'+id,{method:'DELETE'});toast('Product deleted');products()}catch(e){toast(e.message)}}
async function clients(){const cs=await api('/api/clients');$('#content').innerHTML=`<div class="admin-panel"><h3>Our Clients</h3><p class="section-note">Upload authorized client logos. The public website displays them in a smooth horizontal marquee.</p><form id="clientForm" class="admin-form" enctype="multipart/form-data"><input type="hidden" name="id"><div><label>Client Name</label><input name="name" required></div><div><label>Sort Order</label><input name="sort_order" type="number" value="0"></div><div><label>Logo</label><input name="logo" type="file" accept="image/*"></div><div><label><input name="active" type="checkbox" checked> Show client</label></div><div class="full toolbar"><button class="btn btn-primary">Save Client</button><button class="btn btn-success" type="button" id="addClient">＋ Add New Client</button><button class="btn btn-outline" type="button" id="discardClient">Discard Changes</button></div></form></div><div class="admin-panel"><h3>Client List (${cs.length})</h3><table class="admin-table"><thead><tr><th>Logo</th><th>Name</th><th>Status</th><th>Action</th></tr></thead><tbody>${cs.map(c=>`<tr><td><img class="thumb" src="${image(c.logo)}" alt=""></td><td>${esc(c.name)}</td><td>${c.active?'Active':'Hidden'}</td><td><button class="btn-small" onclick='editClient(${JSON.stringify(c).replace(/'/g,"&#39;")})'>Edit</button> <button class="btn-small btn-danger" onclick="deleteClient('${c.id}')">Delete</button></td></tr>`).join('')}</tbody></table></div>`;const f=$('#clientForm');const fresh=()=>{f.reset();f.id.value='';f.active.checked=true;f.sort_order.value=0;f.name.focus();window.scrollTo({top:0,behavior:'smooth'})};$('#addClient').onclick=fresh;$('#discardClient').onclick=fresh;f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),id=fd.get('id');fd.delete('id');try{await api('/api/clients'+(id?'/'+id:''),{method:id?'PUT':'POST',body:fd});toast(id?'Client updated':'Client added');clients()}catch(err){toast(err.message)}};fresh()}
function editClient(c){const f=$('#clientForm');f.id.value=c.id;f.name.value=c.name||'';f.sort_order.value=c.sort_order||0;f.active.checked=!!c.active;window.scrollTo({top:0,behavior:'smooth'})}async function deleteClient(id){if(!confirm('Delete this client?'))return;try{await api('/api/clients/'+id,{method:'DELETE'});toast('Client deleted');clients()}catch(e){toast(e.message)}}
async function events(){const es=await api('/api/events');$('#content').innerHTML=`<div class="admin-panel"><h3>Events & Exhibitions Manager</h3><p class="section-note">Add event/exhibition title, date, location, description and an image. Published events appear on the Events page and the homepage.</p><form id="eventForm" class="admin-form" enctype="multipart/form-data"><input type="hidden" name="id"><div><label>Event / Exhibition Name</label><input name="title" required></div><div><label>Date</label><input name="event_date" placeholder="e.g. 12–15 January 2027"></div><div><label>Location</label><input name="location" placeholder="Venue / City"></div><div><label>Sort Order</label><input name="sort_order" type="number" value="0"></div><div class="full"><label>Image</label><input name="image" type="file" accept="image/*"></div><div class="full"><label>Description</label><textarea name="description"></textarea></div><div><label><input name="active" type="checkbox" checked> Publish on website</label></div><div class="full toolbar"><button class="btn btn-primary">Save Event</button><button class="btn btn-success" type="button" id="addEvent">＋ Add New Event</button><button class="btn btn-outline" type="button" id="discardEvent">Discard Changes</button></div></form></div><div class="admin-panel"><h3>Event List (${es.length})</h3><table class="admin-table"><thead><tr><th>Image</th><th>Event</th><th>Date</th><th>Location</th><th>Status</th><th>Action</th></tr></thead><tbody>${es.map(e=>`<tr><td><img class="thumb" src="${image(e.image)}" alt=""></td><td>${esc(e.title)}</td><td>${esc(e.event_date)}</td><td>${esc(e.location)}</td><td>${e.active?'Published':'Hidden'}</td><td><button class="btn-small" onclick='editEvent(${JSON.stringify(e).replace(/'/g,"&#39;")})'>Edit</button> <button class="btn-small btn-danger" onclick="deleteEvent('${e.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan="6">No events added.</td></tr>'}</tbody></table></div>`;const f=$('#eventForm');const fresh=()=>{f.reset();f.id.value='';f.active.checked=true;f.sort_order.value=0;f.title.focus();window.scrollTo({top:0,behavior:'smooth'})};$('#addEvent').onclick=fresh;$('#discardEvent').onclick=fresh;f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),id=fd.get('id');fd.delete('id');try{await api('/api/events'+(id?'/'+id:''),{method:id?'PUT':'POST',body:fd});toast(id?'Event updated':'Event added');events()}catch(err){toast(err.message)}};fresh()}
function editEvent(e){const f=$('#eventForm');f.id.value=e.id;f.title.value=e.title||'';f.event_date.value=e.event_date||'';f.location.value=e.location||'';f.sort_order.value=e.sort_order||0;f.description.value=e.description||'';f.active.checked=!!e.active;window.scrollTo({top:0,behavior:'smooth'})}async function deleteEvent(id){if(!confirm('Delete this event?'))return;try{await api('/api/events/'+id,{method:'DELETE'});toast('Event deleted');events()}catch(e){toast(e.message)}}
async function enquiries(){const es=await api('/api/enquiries');$('#content').innerHTML=`<div class="admin-panel"><h3>Customer Enquiries</h3><table class="admin-table"><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Product</th><th>Message</th><th>Date</th><th></th></tr></thead><tbody>${es.map(e=>`<tr><td>${esc(e.name)}</td><td>${esc(e.email)}</td><td>${esc(e.phone)}</td><td>${esc(e.product)}</td><td>${esc(e.message)}</td><td>${esc(e.created_at)}</td><td><button class="btn-small btn-danger" onclick="deleteEnquiry('${e.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan="7">No enquiries yet.</td></tr>'}</tbody></table></div>`}async function deleteEnquiry(id){if(!confirm('Delete this enquiry?'))return;await api('/api/enquiries/'+id,{method:'DELETE'});toast('Enquiry deleted');loadTab('enquiries')}
document.addEventListener('DOMContentLoaded',()=>{$('#loginForm').addEventListener('submit',login);$('#logout').onclick=logout;document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>loadTab(b.dataset.tab));if(token){showApp();loadTab('dashboard')}});
